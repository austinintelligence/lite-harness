[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[project]
name = "lite-harness-sdk"
version = "0.1.0a0"
description = "Thin Python client for the Lite-Harness REST and replayable SSE API"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/lite_harness"]
